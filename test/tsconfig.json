{
	"extends": "../tsconfig.json",
	"compilerOptions": {
		"types": ["@cloudflare/workers-types/experimental", "@cloudflare/vitest-pool-workers"]
	},
	// I had to modify the second include, the default is ../src/env.d.ts
	"include": ["./**/*.ts", "../worker-configuration.d.ts"],
	"exclude": []
}
